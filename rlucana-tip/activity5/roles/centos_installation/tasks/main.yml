---
# tasks file for roles/centos_installation

  - name: Java Installation
    yum:
      name: java-1.8.0-openjdk
      state: present
      update_cache: yes

  - name: Installation of Ansible, boto, and openstack Using pip
    pip:
      name: 
        - ansible
        - boto
        - python-openstackclient
