---
# tasks file for roles/clone_prelimExam_centos

- name: Install Git
  yum:
    name: git
    state: latest
    update_cache: yes

- name: Clone Prelim Exam Repository
  git:
    repo: "{{ git_repo }}"
    dest: "{{ git_dest }}"
    version: "{{ git_version }}"
